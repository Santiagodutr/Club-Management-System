---
import AdminLayout from '../../layouts/AdminLayout.astro'

export const prerender = false
---

<AdminLayout title="Espacios - Club del Meta" active="espacios">
  <section class="panel">
    <header class="panel__header">
      <div>
        <p class="eyebrow">Administración</p>
        <h1>Espacios</h1>
        <p class="muted">Gestiona los salones del club: edita información, imágenes, disposiciones y precios.</p>
      </div>
      <div class="actions">
        <button id="btnRefrescarEspacios" class="btn secondary">Refrescar</button>
        <button id="btnPublicarCambios" class="btn" style="background: #16a34a; color: white; display: none;">Publicar Cambios</button>
        <button id="btnNuevoEspacio" class="btn primary">+ Nuevo Salón</button>
      </div>
    </header>

    <div class="grid">
      <section class="card">
        <div class="card__header">
          <div>
            <p class="eyebrow">Salones</p>
            <h2>Lista de espacios</h2>
          </div>
        </div>
        <div id="espaciosList" class="espacios-list">
          <div class="placeholder">Cargando espacios...</div>
        </div>
      </section>

    </div>
  </section>

  <!-- Modal de confirmación para eliminar -->
  <div id="confirmModal" class="modal-overlay hidden">
    <div class="modal-confirm">
      <div class="modal-confirm__icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="15" y1="9" x2="9" y2="15"></line>
          <line x1="9" y1="9" x2="15" y2="15"></line>
        </svg>
      </div>
      <h3 class="modal-confirm__title">¿Eliminar espacio?</h3>
      <p class="modal-confirm__message">Esta acción no se puede deshacer. El espacio <strong id="espacioEliminarNombre"></strong> será eliminado permanentemente con todas sus configuraciones.</p>
      <div class="modal-confirm__actions">
        <button id="btnCancelarEliminar" class="btn ghost">Cancelar</button>
        <button id="btnConfirmarEliminar" class="btn danger">Eliminar</button>
      </div>
    </div>
  </div>

  <!-- Modal de publicar cambios -->
  <div id="publicarModal" class="modal-overlay hidden">
    <div class="modal-publicar">
      <h3 class="modal-publicar__title">Publicar Cambios</h3>
      
      <div class="modal-publicar__info">
        <p><strong>✓</strong> Esto actualizará el sitio web público con todos los cambios realizados.</p>
        <p><strong>⏱️</strong> El proceso toma 2-3 minutos y el sitio no se cae durante la publicación.</p>
      </div>

      <div id="mensajePublicar" class="modal-publicar__mensaje hidden"></div>

      <div class="modal-publicar__actions">
        <button id="btnCancelarPublicar" class="btn ghost">Cancelar</button>
        <button id="btnConfirmarPublicar" class="btn" style="background: #16a34a; color: white;">Publicar Ahora</button>
      </div>
    </div>
  </div>
</AdminLayout>

<script>
import '@scripts/espacios/lista.ts'
</script>

<style>
  /* All base styles (card, btn, label, etc.) come from AdminLayout.astro */
  .panel { width: 100%; max-width: 1200px; }
  .grid { display: grid; grid-template-columns: 1fr; gap: 1.25rem; margin-top: 1.5rem; }
  .espacios-list { display: grid; gap: 0.75rem; }

  :global(.espacio-card) {
    display: grid;
    grid-template-columns: 120px 1fr auto;
    gap: 1rem;
    align-items: center;
    padding: 1rem;
    border: 1px solid var(--border);
    border-radius: 16px;
    background: var(--card-bg);
    backdrop-filter: blur(16px);
    transition: all 0.25s ease;
    overflow: hidden;
  }

  :global(.espacio-card:hover) { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(0,0,0,0.2); border-color: var(--border-hover); }

  :global(.espacio-card img) { width: 120px; height: 90px; object-fit: cover; border-radius: 10px; }

  :global(.espacio-info) { display: grid; gap: 0.5rem; min-width: 0; }

  :global(.espacio-tag) { display: inline-flex; align-items: center; gap: 0.3rem; padding: 0.25rem 0.6rem; border-radius: 999px; font-weight: 600; font-size: 0.75rem; width: fit-content; }
  :global(.destacado-tag) { background: rgba(251, 191, 36, 0.1); color: #fbbf24; border: 1px solid rgba(251, 191, 36, 0.2); }

  :global(.espacio-nombre) { font-weight: 700; color: var(--text); font-size: 1.05rem; margin: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  :global(.espacio-meta) { display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap; color: var(--text-muted); font-size: 0.85rem; }
  :global(.espacio-actions) { display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap; }
  :global(.espacio-actions .btn) { font-size: 0.8rem; padding: 0.5rem 0.85rem; }

  /* Config card for expanded view */
  :global(.config-card) { display: grid; grid-template-columns: 1fr auto auto; gap: 0.75rem; align-items: center; padding: 0.85rem; border: 1px solid var(--border); border-radius: 12px; background: var(--surface); }
  :global(.config-card .capacidad) {
    width: 130px; text-align: center; border-radius: 10px;
    border: 1px solid var(--input-border); background: var(--input-bg);
    padding: 0.5rem; font-weight: 700; color: var(--text);
    font-family: inherit; font-size: inherit;
    transition: border-color 0.15s; box-sizing: border-box;
  }
  :global(.config-card .capacidad:focus) { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px rgba(59,130,246,0.15); }

  /* Modal publicar */
  .modal-publicar__info { background: rgba(52, 211, 153, 0.06); border-left: 3px solid #34d399; padding: 1rem; border-radius: 0 10px 10px 0; }
  .modal-publicar__info p { margin: 0 0 0.5rem; color: var(--text-muted); font-size: 0.9rem; }
  .modal-publicar__info p:last-child { margin: 0; }
  .modal-publicar__mensaje { padding: 0.85rem 1rem; border-radius: 10px; font-size: 0.9rem; text-align: center; margin-bottom: 1rem; }
  .modal-publicar__mensaje.exito { background: rgba(52, 211, 153, 0.1); color: #34d399; }
  .modal-publicar__mensaje.error { background: rgba(248, 113, 113, 0.1); color: #f87171; }
  .modal-publicar__mensaje.info { background: rgba(59, 130, 246, 0.1); color: #93c5fd; }

  :global(.link) {
    display: inline-flex; align-items: center; gap: 0.4rem;
    padding: 0.45rem 0.75rem; border-radius: 10px; font: inherit; font-size: 0.8rem;
    font-weight: 600; cursor: pointer; border: 1px solid var(--border);
    background: var(--surface); color: var(--text-muted); transition: all 0.2s ease;
    text-decoration: none;
  }
  :global(.link:hover) { background: var(--surface-hover); color: var(--text); }
  :global(.link.danger) { background: rgba(248,113,113,0.08); color: #f87171; border-color: rgba(248,113,113,0.2); }

  @media (max-width: 900px) {
    :global(.espacio-card) { grid-template-columns: 1fr; }
    :global(.espacio-card img) { width: 100%; height: 200px; }
    :global(.espacio-actions) { flex-direction: column; width: 100%; }
    :global(.espacio-actions .btn) { width: 100%; justify-content: center; }
  }
</style>