---
import BaseLayout from '../layouts/BaseLayout.astro'
import Navbar from '../components/Navbar.astro'
import heroBackground from 'src/assets/fondoclub.png'

// Obtener número de WhatsApp desde la base de datos
 
---

<BaseLayout title="Contacto - Club del Meta" hideNavbar fullWidth>
  <section class="contact-hero">
    <div
      class="contact-hero__background"
      style={`background-image: linear-gradient(rgba(4, 14, 32, 0.4), rgba(4, 14, 32, 0.7)), url(${heroBackground.src});`}
    ></div>
    
    <div class="navbar-wrapper">
      <Navbar />
    </div>

    <div class="contact-hero__content">
      <div class="contact-hero__badge">Atención Personalizada</div>
      <h1>
        <span class="title-top">ESTAMOS A TU</span>
        <span class="title-main">DISPOSICIÓN</span>
      </h1>
      <p>
        ¿Tienes un evento en mente o deseas conocer más sobre nuestra comunidad? 
        Nuestro equipo está listo para brindarte la mejor asesoría en el corazón de Villavicencio.
      </p>
    </div>
  </section>

  <section class="contact-content">
    <div class="contact-container">
      <div class="contact-info-section">
        <div class="section-header">
          <h2>Canales de Contacto</h2>
          <p>Elige el medio que prefieras para comunicarte con nosotros</p>
        </div>
        
        <div class="info-grid">
          <div class="info-card" style="--card-index: 1">
            <div class="info-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
              </svg>
            </div>
            <h3>Líneas Directas</h3>
            <div class="info-details">
              <p><span>General:</span> (8) 6 64 11 15</p>
              <p><span>WhatsApp:</span> 312 4347893</p>
              <p><span>Eventos:</span> 310 8874473</p>
            </div>
          </div>

          <div class="info-card" style="--card-index: 2">
            <div class="info-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                <polyline points="22,6 12,13 2,6"></polyline>
              </svg>
            </div>
            <h3>Correo Electrónico</h3>
            <div class="info-details">
              <p><a href="mailto:comunicaciones@clubelmeta.com">comunicaciones@clubelmeta.com</a></p>
              <p><a href="mailto:secretaria@clubelmeta.com">secretaria@clubelmeta.com</a></p>
            </div>
          </div>

          <div class="info-card" style="--card-index: 3">
            <div class="info-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                <circle cx="12" cy="10" r="3"></circle>
              </svg>
            </div>
            <h3>Ubicación</h3>
            <div class="info-details">
              <p>Calle 48A Carrera 30</p>
              <p>Barrio Ciudad Oriental</p>
              <p>Villavicencio - Meta</p>
            </div>
          </div>

          <div class="info-card" style="--card-index: 4">
            <div class="info-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
            </div>
            <h3>Horarios</h3>
            <div class="info-details">
              <p>Lunes a Viernes: 8:00 AM - 6:00 PM</p>
              <p>Sábados: 9:00 AM - 2:00 PM</p>
              <p>Domingos: Cerrado</p>
            </div>
          </div>
        </div>
      </div>

      <div class="map-section">
        <div class="section-header">
          <h2>Cómo Llegar</h2>
          <p>Visítanos en nuestras instalaciones de primer nivel</p>
        </div>
        <div class="map-container">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d1989.645188052661!2d-73.63941951079238!3d4.163210806773625!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8e3e2d9252257e01%3A0x13a9394f0ef8b23d!2sClub%20Meta!5e0!3m2!1ses!2sco!4v1762378314013!5m2!1ses!2sco"
            width="100%"
            height="500"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
          ></iframe>
        </div>
      </div>
    </div>
  </section>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Outfit:wght@300;400;600;700&display=swap');

    .contact-hero {
      position: relative;
      min-height: 80vh;
      display: flex;
      flex-direction: column;
      color: #ffffff;
      overflow: hidden;
      margin: 0;
      width: 100%;
    }

    .contact-hero__background {
      position: absolute;
      inset: 0;
      background-size: cover;
      background-position: center;
      width: 100%;
      transform: scale(1.05);
      animation: heroZoom 20s infinite alternate;
    }

    @keyframes heroZoom {
      from { transform: scale(1.05); }
      to { transform: scale(1.2); }
    }

    .navbar-wrapper {
      position: relative;
      z-index: 10;
    }

    .navbar-wrapper :global(.navbar) {
      background: transparent !important;
      box-shadow: none !important;
      padding-top: 2rem !important;
    }

    .navbar-wrapper :global(.navbar__link) {
      color: #ffffff !important;
    }

    .contact-hero__content {
      position: relative;
      text-align: center;
      max-width: 900px;
      margin: auto;
      padding: 3rem 2rem;
      z-index: 1;
    }

    .contact-hero__badge {
      display: inline-block;
      padding: 0.5rem 1.25rem;
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 99px;
      font-family: 'Outfit', sans-serif;
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      margin-bottom: 2rem;
    }

    .contact-hero h1 {
      font-family: 'Bebas Neue', sans-serif;
      line-height: 0.9;
      margin: 0 0 2rem;
    }

    .title-top {
      display: block;
      font-size: clamp(2rem, 5vw, 3rem);
      color: rgba(255, 255, 255, 0.7);
      letter-spacing: 0.1em;
    }

    .title-main {
      display: block;
      font-size: clamp(4rem, 12vw, 8rem);
      color: #ffffff;
      letter-spacing: 0.02em;
    }

    .contact-hero p {
      font-family: 'Outfit', sans-serif;
      font-size: clamp(1.1rem, 2vw, 1.35rem);
      line-height: 1.7;
      color: rgba(255, 255, 255, 0.9);
      max-width: 750px;
      margin: 0 auto;
    }

    .contact-content {
      padding: 8rem 2rem;
      background: #fdfdfd;
    }

    .contact-container {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 8rem;
    }

    .section-header {
      text-align: center;
      margin-bottom: 5rem;
    }

    .section-header h2 {
      font-family: 'Bebas Neue', sans-serif;
      font-size: clamp(3rem, 6vw, 4rem);
      color: #041a4a;
      margin: 0 0 1rem;
    }

    .section-header p {
      font-family: 'Outfit', sans-serif;
      color: #94a3b8;
      font-size: 1.1rem;
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2.5rem;
    }

    .info-card {
      background: white;
      padding: 3.5rem 2.5rem;
      border-radius: 32px;
      text-align: left;
      border: 1px solid #f1f5f9;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.02);
      transition: all 0.4s cubic-bezier(0.2, 0, 0.2, 1);
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 0.8s ease-out forwards;
      animation-delay: calc(var(--card-index) * 0.15s);
    }

    .info-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 40px 60px rgba(4, 26, 74, 0.08);
      border-color: #e2e8f0;
    }

    .info-icon {
      width: 64px;
      height: 64px;
      background: #f8fafc;
      color: #0a4ba5;
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 2.5rem;
      transition: all 0.4s ease;
    }

    .info-card:hover .info-icon {
      background: #0a4ba5;
      color: white;
      transform: rotate(10deg);
    }

    .info-card h3 {
      font-family: 'Outfit', sans-serif;
      font-size: 1.5rem;
      font-weight: 700;
      margin: 0 0 1.5rem;
      color: #041a4a;
    }

    .info-details p {
      font-family: 'Outfit', sans-serif;
      margin: 0.75rem 0;
      color: #64748b;
      line-height: 1.5;
      font-size: 1.05rem;
    }

    .info-details span {
      font-weight: 700;
      color: #475569;
      margin-right: 0.5rem;
    }

    .info-details a {
      color: #0a4ba5;
      text-decoration: none;
      font-weight: 600;
      border-bottom: 1.5px solid transparent;
      transition: all 0.2s ease;
    }

    .info-details a:hover {
      border-bottom-color: #0a4ba5;
    }

    .map-container {
      border-radius: 32px;
      overflow: hidden;
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.08);
      border: 8px solid white;
    }

    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 768px) {
      .contact-content {
        padding: 5rem 1.5rem;
      }

      .contact-container {
        gap: 5rem;
      }

      .info-card {
        padding: 2.5rem 2rem;
      }

      .title-main {
        font-size: 4rem;
      }
    }
  </style>
</BaseLayout>
